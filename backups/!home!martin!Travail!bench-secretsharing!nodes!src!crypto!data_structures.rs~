use super::include::*;
use blstrs::G1Projective;
use serde::{Serialise, Deserialise};

#[derive(Clone, Serialise, Deserialise)]
pub struct Share {
    share: [Scalar; 2],
    index: u16
}

impl Share {

    pub fn new(index: u16, share: [Scalar; 2]) -> Self {
        Share {share, index}
    }
    
    pub fn index(&self) -> u16 {
        self.index
    }
    

    pub fn read(buf: &[u8]) -> (Self, usize) {
        todo!()
    }
}

#[derive(Clone)]
pub struct Commitment {
    comms: Vec<G1Projective>,
    base: [G1Projective; 2]
}


impl Commitment {

    pub fn new(base: [G1Projective; 2], comms: Vec<G1Projective>) -> Self {
        Commitment {
            comms,
            base
        }
    }
    
    pub fn append(&self, buf: &mut Vec<u8>) -> usize {
        todo!()
    }

    pub fn read(buf: &[u8]) -> (Commitment, usize) {
        todo!()
    }
}
