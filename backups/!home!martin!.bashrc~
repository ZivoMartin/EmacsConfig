>#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
alias discord='flatpak run de.shorsh.discord-screenaudio'
PS1='[\u@\h \W]\$ '
alias goz='cd ~/Travail/Zilang/zipiler'
alias proj='cd ~/Travail/bench-secretsharing'
export PATH=$PATH:/home/martin/.config/emacs/bin
alias firefox='echo "C est pour jouer ou travailler ?? Fais gaffe je te surveilles"'
alias cd="ls"

