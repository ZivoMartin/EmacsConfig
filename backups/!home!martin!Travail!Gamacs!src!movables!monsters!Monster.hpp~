#ifndef SAFE_MONSTER
#define SAFE_MONSTER

#include "Movable.hpp"

class Env;

class Monster : public Movable {

public:
	Monster(Env* env, SDL_Point pos);
	~Monster();
	void action();
	void draw() override;
};

#endif
