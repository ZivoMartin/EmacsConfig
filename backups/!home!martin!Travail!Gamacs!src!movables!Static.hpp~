#ifndef SAFE_SPRITE
#define SAFE_SPRITE
#include "../include.hpp"
#include "Collider.hpp"

#include "../Source.hpp"

class Env;
class Monster;
class Player;

class Sprite : virtual public Source {

public:
	Sprite(Env* env, const char* img_path, float fx, float fy);
	~Sprite();
	virtual void update();
	virtual void draw(int x, int y);
	virtual void draw();
	SDL_Texture* get_text();


	float fx();
	float fy();
	
private:
	Env* env;
	SDL_Texture* sprite_sheet;
	SDL_FPoint factors;

};


#endif
